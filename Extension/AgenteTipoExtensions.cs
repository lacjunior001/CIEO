using CIEO.Models;
using CIEO.Models.EDocs;

namespace CIEO.Extension
{
    public static class AgenteTipoExtensions
    {
        /*
        public static AgenteTipoEnum AgenteTipoToEnum(this string texto)
        {
            texto = texto.ToLower();
            texto = char.ToUpper(texto[0]) + texto.Substring(1);

            switch (texto)
            {
                case "PessoaF":
                    return AgenteTipoEnum.Cidadao;
                case "Publico":
                    return AgenteTipoEnum.Papel;
                case "Organizacional":
                    return AgenteTipoEnum.Organizacao;
                case "Sigiloso":
                    return AgenteTipoEnum.Grupo;
                case "Classificado":
                    return AgenteTipoEnum.NaoIdentificado;
                case "Classificado":
                    return AgenteTipoEnum.NaoIdentificado;
                default:
                    return AgenteTipoEnum.Erro;
            }
        }*/
    }
}